modelo de invetario completo

=============== O mais utilizado ===============
# Aqui eu não preciso declarar o grupo padrão all e nem o ungrouped
# Um grupo normal
[debian]
192.168.1.[100:110]
servidor[01:05]
debian01 ansible_host=192.168.1.100 ansible_user=root ansible_port=22
  # ansible_host=192.168.1.100 --> Aqui estou falando que o debian01 responde no IP informado na variavel
  # ansible_user=root --> Estou declarando que o usuário de login será o root ou qualquer que eu escolha
  # ansible_port=22 --> Definindo a porta de comunicação
  # ansible_ssh_port=22 --> Definindo a porta de comunicação
  # ansible_ssh_private_key_file=[/caminho/chave] --> Caminho da minha chave privada

[redhat]
192.168.1.101

[linux:children]
debian
redhat
  # Um grupo chamado linux e possui como filhos 2 grupos. Na chamada só mencionar linux que ele chama todos os filhos

[debian:vars]
http_port=8080
teste=443

[redhat:vars]
squid_port=3128

[linux:vars]
ansible_port=22
ansible_ssh_user=root

  # variaveis globais para cada grupo
    # Para eu testar cada variavel nos grupos: ansible debian -m debug -a var=http_port



=============== esse modelo utiliza o padrão yaml ===============
# Aqui eu preciso declarar o grupo padrão all

all:
  hosts:
  children:
      debian:
        hosts:
          192.168.1.100:
            http_port: 8080
            https_port: 443
      redhat:
        hosts:
          192.168.1.101:
            squid_port: 3128
        vars:
          ansible_port: 22
          ansible_ssh_user: root
      linux:
        children:
          redhat:
          debian:




